# AI-Driven Development Templates

このリポジトリはAI駆動開発のテンプレート集です。

## 構成

- `nextjs/` - Next.js フロントエンド開発テンプレート
- `springboot/` - Spring Boot バックエンド開発テンプレート

## 共通ルール

### 言語

- 日本語でコミュニケーションすること
- コードのコメントは英語で書くこと
- コミットメッセージは英語で書くこと

### 設計原則

以下のSOLID原則を各テンプレートに適用する。具体的な適用方法は各テンプレートの CLAUDE.md を参照。

- **単一責任 (SRP)**: 1つのモジュール・クラス・関数は1つの責務だけを持つ。変更理由が複数あるなら分割する
- **開放閉鎖 (OCP)**: 既存コードを修正せず、拡張で機能を追加できる設計にする。共通インターフェースやcompositionを活用する
- **リスコフ置換 (LSP)**: 派生型は基底型の契約を破らない。インターフェースを実装する際、呼び出し側の期待を裏切らない
- **インターフェース分離 (ISP)**: 使わないメソッド・propsへの依存を強制しない。インターフェースは用途ごとに小さく分ける
- **依存性逆転 (DIP)**: 上位モジュールは下位モジュールに直接依存しない。抽象（インターフェース）に依存する

### コーディング規約

- 早期リターンを使い、ネストを浅く保つ
- マジックナンバー・マジックストリングは定数化する
- 関数・メソッドは短く保つ（目安: 30行以内）
- 副作用のある処理と純粋な計算を分離する
- エラーは握り潰さず、適切にハンドリングする

### テスト

- テストは実装と同時に書く。後回しにしない
- テストメソッド名は「何を」「どの条件で」「どうなるか」を表現する
- 外部依存（DB、API）を伴う統合テストとユニットテストを明確に分ける

### Git

- コミットは1つの論理的変更ごとに作成する
- ブランチ名: `feature/xxx`, `fix/xxx`, `refactor/xxx`
