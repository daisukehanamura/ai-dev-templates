# AI-Driven Development Templates

このリポジトリはAI駆動開発のテンプレート集です。

## 構成

- `docs/specs/` - 仕様書
- `nextjs/` - Next.js フロントエンド開発テンプレート
- `springboot/` - Spring Boot バックエンド開発テンプレート

## 共通ルール

### 言語

- 日本語でコミュニケーションすること
- コードのコメントは英語で書くこと
- コミットメッセージは英語で書くこと

### 設計原則

SOLID原則に従う。各テンプレートの CLAUDE.md にプロジェクト固有の適用ルールを定義。

### コーディング規約

- 早期リターンでネストを浅く保つ
- マジックナンバー・マジックストリングは定数化する
- 副作用のある処理と純粋な計算を分離する
- エラーは握り潰さず、適切にハンドリングする
- 関数・メソッドの長さやパラメータ数は linter で制御する（ルールは各テンプレートで定義）

### テスト

- テストは実装と同時に書く
- テスト名は「何を」「どの条件で」「どうなるか」を表現する
- ユニットテストと統合テストを分離する。具体的なフレームワークやディレクトリは各テンプレートで定義

### 開発プロセス

すべての機能開発・変更は「仕様書 → 実装」の順で進める。

#### ワークフロー

1. **仕様書作成**: ユーザーとの会話から要件を整理し、`docs/specs/` に仕様書を作成する
2. **レビュー**: ユーザーが仕様書を確認・承認する
3. **実装**: 承認された仕様書に基づいて実装する
4. **仕様書更新**: 実装中に仕様が変わった場合、仕様書も更新する

#### 仕様書の配置

```
docs/
  specs/
    XXXX-feature-name.md      # 個別の仕様書（連番 + kebab-case）
```

#### 仕様書のフォーマット

```markdown
# タイトル

## 概要
この機能が何を解決するか（1-2文）

## 背景・動機
なぜこの変更が必要か

## 要件
- [ ] 要件1
- [ ] 要件2

## 設計
### ディレクトリ構成の変更
### 主要コンポーネント/クラス
### データフロー（必要な場合）

## 影響範囲
変更が影響する既存機能・ファイル

## 未決事項
決まっていない点があれば記載
```

#### ルール

- 仕様書なしに機能開発を始めない
- 会話で決まったことは仕様書に落とす。会話はコンテキストから消えるが、仕様書は残る
- 仕様書は実装の「信頼できる情報源 (single source of truth)」として扱う
- 小さなバグ修正やリファクタリングには仕様書不要。目安: 複数ファイルにまたがる変更や新機能は仕様書を書く

### Git

- コミットは1つの論理的変更ごとに作成する
- ブランチ名: `feature/xxx`, `fix/xxx`, `refactor/xxx`

### トークン効率化

AI との作業においてトークン消費を最小化するためのルール。

#### 応答

- 作業結果の要約や振り返りは不要。差分を見ればわかる
- 「〜を読みます」「〜を確認します」等の予告は不要。黙って実行する
- 選択肢の提示は箇条書きで簡潔に。長い説明文を付けない
- コード以外の出力は最小限にする

#### ファイル読み取り

- CLAUDE.md にディレクトリ構成が記載されている。構成を探索するためだけにファイルを読まない
- 大きなファイルは `offset` / `limit` で必要な範囲だけ読む
- 同一会話で既に読んだファイルを再度読まない
- Glob/Grep で対象を絞ってから Read する。手当たり次第に読まない

#### コード生成

- 動くコードを最短経路で書く。過剰な防御的コーディング・過剰なコメント・過剰な型注釈を避ける
- 既存コードの修正は Edit で差分だけ送る。ファイル全体を Write で書き直さない
- 依頼されていないリファクタリング・ドキュメント追加・テスト追加をしない
- 1つのタスクに複数ファイルの変更が必要な場合、並列で実行する

#### サブエージェント

- 単純な検索に Agent を使わない。Glob/Grep で十分なケースが大半
- Agent を使う場合は目的を明確にし、結果を簡潔に受け取る
